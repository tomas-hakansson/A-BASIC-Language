using ConsoleControlLibrary;

namespace A_BASIC_Language.Gui.WinForms;

public class LoadForm : ConsoleForm
{
    public LoadForm(nint handle, ConsoleControl parentConsole) : base(handle, parentConsole)
    {
    }
}